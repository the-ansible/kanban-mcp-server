{
  "name": "kanban-mcp-server",
  "version": "1.0.1",
  "description": "MCP server for Life System Kanban API",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "start:combined": "node start.js",
    "test": "vitest",
    "test:run": "vitest --run"
  },
  "keywords": [
    "mcp",
    "kanban",
    "model-context-protocol"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "typescript": "^5.3.3",
    "tsx": "^4.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "vitest": "^1.2.0"
  }
}
